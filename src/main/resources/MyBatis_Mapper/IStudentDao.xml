<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pet.dao.IStudentDao">
  <select id="findStudenInfo" resultType="pet.model.Student">
    	select name, student_id as age from student;
  </select>
</mapper>